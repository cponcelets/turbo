diff --git a/include/lala/XCSP3_parser.hpp b/include/lala/XCSP3_parser.hpp
index cc9799c..1bedf28 100644
--- a/include/lala/XCSP3_parser.hpp
+++ b/include/lala/XCSP3_parser.hpp
@@ -255,7 +255,7 @@ void displayList(vector<T> &list, string separator = " ") {
     cout << endl;
 }
 
-void displayList(vector<XVariable *> &list, string separator = " ") {
+inline void displayList(vector<XVariable *> &list, string separator = " ") {
     if(list.size() > 8) {
         for(int i = 0; i < 3; i++)
             cout << list[i]->id << separator;
@@ -651,7 +651,7 @@ void XCSP3_turbo_callbacks<Allocator>::buildConstraintIntension(string id, Tree
   constraints.push_back(make_formula(tree->root));
 }
 
-lala::Sig convert(OrderType op) {
+inline lala::Sig convert(OrderType op) {
   switch(op) {
     case LT: return lala::LT;
     case LE: return lala::LEQ;
diff --git a/include/lala/solver_output.hpp b/include/lala/solver_output.hpp
index 228cd71..6eacd3d 100644
--- a/include/lala/solver_output.hpp
+++ b/include/lala/solver_output.hpp
@@ -97,6 +97,10 @@ public:
     output_vars.push_back(var_name);
   }
 
+  const auto get_output_vars() const {
+    return battery::tuple(output_vars, output_arrays);
+  }
+
   class SimplifierIdentity {
     template <class Alloc, class B, class Env>
     CUDA void print_variable(const LVar<Alloc>& vname, const Env& benv, const B& b) const {
@@ -105,6 +109,11 @@ public:
     }
   };
 
+  template <class Alloc, class Env, class A, class S>
+  CUDA auto get_value_of(const LVar<Alloc>& vname, const Env& env, const A& sol, const S& simplifier = SimplifierIdentity{}) const {
+    return simplifier.get_value_of(vname, env, sol);
+  }
+
   template <class Env, class A, class S>
   CUDA void print_solution_flatzinc(const Env& env, const A& sol, const S& simplifier = SimplifierIdentity{}) const {
     for(int i = 0; i < output_vars.size(); ++i) {
